[package]
name = "shell_vm"
version = "0.1.0"
authors = ["Wez Furlong"]
edition = "2018"

[dependencies]
failure = "0.1"
failure_derive = "0.1"
filedescriptor = { path = "../filedescriptor" }
globwalk = "0.7.0"
lazy_static = "1.3"

[dev-dependencies]
pretty_assertions = "0.6"
